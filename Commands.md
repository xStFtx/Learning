# Usefull comamnds I learnt through this:
See how long it took to execute code
```
g++ -O2 (.cpp file) -o (bin)
```
Then run:
```
time ./(bin)
```

-O2 means optimize the code. 
-O3 is the max optimization. It adds loop unrolling, function inlining, vectorization, aggressive instruction scheduling.

